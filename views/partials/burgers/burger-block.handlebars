<li>
	{{name}}

	<button class="change-burger" data-id="{{id}}" data-newdevour="{{sleep}}">
		{{#if sleep}}DEVOUR TIME!{{else}}NO EATING!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
